apiVersion: core.kubebrowser.io/v1alpha1
kind: BrowserSystem
metadata:
  labels:
    app.kubernetes.io/name: operator
    app.kubernetes.io/managed-by: kustomize
  name: browsersystem-sample
spec:
  # TODO(user): Add fields here
